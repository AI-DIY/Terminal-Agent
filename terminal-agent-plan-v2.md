# Terminal-Agent V2：MVP 产品设计

## 1. 产品目标

Terminal-Agent V2 是一个以 Vue + Electron 构建的桌面 SSH 智能体。它不再复用、嵌入或调用 PuTTY 的 C 语言连接内核，而是在 Electron 内实现自己的连接、终端、观察、结构化事实存储和 AI 调度能力。

产品的核心不是把一批预设诊断或运维任务做成菜单，而是让用户给出目标后，由 AI 结合当前终端上下文与已知主机事实，拆解、编排并推进这个目标。它既可以处理单机，也可以协调多个会话。

MVP 采用一条清晰的主流程：

```text
连接
  -> 终端与观察
  -> 主机事实库
  -> AI 调度与行动
```

每一步都有单一职责：先建立会话，再获取不改变环境的事实，随后直接存储和复用结构化事实，最后由 AI 围绕用户目标作出行动安排。

## 2. 技术方向与边界

### 2.1 单一桌面技术栈

* **界面层**：Vue 3 + TypeScript，提供连接页、会话标签、终端、聊天与多机会话工作台。
* **桌面与受控能力层**：Electron 主进程负责 SSH 连接、终端数据流、AccessClient 启动参数解析、私钥处理、AI 行动调度和本地结构化数据。
* **SSH 与终端组件**：后续实施以 `ssh2` 承担 Electron 主进程的 SSH 能力，以 `xterm.js` 呈现终端。该选择保留实现空间，但不改变本设计的产品边界。

Vue 渲染层不能直接获得 SSH 或系统执行能力。所有可能发往远端的 AI 行动都由 Electron 主进程的受控执行入口处理，以确保驾驶模式不会被界面或模型绕过。

### 2.2 不做 PuTTY 内核迁移

V2 不把 PuTTY 的 C 代码改写、封装或作为子进程调用。V2 以 JavaScript/TypeScript 的 Electron 能力重新实现所需 SSH 体验，并兼容 AccessClient 对 PuTTY 命令行启动形式的调用约定。

这意味着“兼容 AccessClient”只兼容其启动契约，不实现供应商堡垒机协议，也不要求用户手工配置堡垒机跳转。

### 2.3 大模型连接

`设置 > 大模型连接` 只支持 **OpenAI Chat Completions** 协议，不提供 Ollama 专属 API、模型发现或运行器集成。

配置项包括 Chat Completions 端点、模型名称、API Key 与上下文长度；请求使用 `POST /v1/chat/completions`，并支持流式回复。端点可采用 OpenAI 官方地址，也可采用遵循同一 Chat Completions 协议的服务地址。

模型连接设置在当前用户本地保存；API Key 使用操作系统提供的加密存储保护。它是模型连接所需的最小配置，不扩展为通用凭据保险库。

## 3. 连接

连接页提供三个**并列**入口；它们不是多跳配置，也不是必须按顺序组合的三段链路。

| 入口 | 用户提供的信息 | 结果 |
| --- | --- | --- |
| 用户名 + 密码 | 主机名、端口、用户名、密码 | 建立直接 SSH 会话 |
| 私钥 + 口令 | 主机名、端口、用户名、私钥文件、可选私钥口令 | 建立直接 SSH 会话 |
| AccessClient 启动兼容 | 由 AccessClient 启动本应用并传入参数 | 解析参数后建立对应会话 |

### 3.1 密码与私钥登录

必须支持原始用户名密码，以及私钥登录。私钥范围包含：

* OpenSSH；
* PEM；
* PKCS#1 / PKCS#8；
* PuTTY `.ppk`；
* 加密私钥与私钥口令。

密码、私钥口令和 AccessClient 传入的临时密码只在当前连接所需的内存中使用。MVP 不建设持久化凭据保险库，也不把这些秘密写入主机事实、持久化配置、日志或 AI 上下文。

### 3.2 AccessClient 启动兼容

这里的真实链路是：

```text
AccessClient 自行完成授权与连接准备
  -> 使用 PuTTY 兼容参数启动 Terminal-Agent
  -> Terminal-Agent 解析参数并打开对应会话
```

Terminal-Agent 需要兼容以下启动形式：

```text
Terminal-Agent.exe @session-name
Terminal-Agent.exe -load session-name
Terminal-Agent.exe -load "tmp:C:\\path\\to\\session.conf" -pw "password"
Terminal-Agent.exe -raw -P local-port
```

兼容规则：

* 解析 `tmp:` 指向的 UTF-8 `key=value` 临时会话文件；
* 识别 `HostName`、`PortNumber`、`UserName`、`Protocol`、`WinTitle`、`TermWidth`、`TermHeight`、`LineCodePage` 等通用字段；
* 忽略 `mode`、`websid` 等供应商私有且本应用不需要的字段；
* 对“有有效端口、Raw 协议、但没有主机名”的兼容会话，内部以 `127.0.0.1` 作为连接目标；这只是 AccessClient 兼容行为，不在 UI 中增加手工 `127.0.0.1:端口` 的第四种连接方式；
* 支持 Electron 的 `second-instance` 处理：AccessClient 后续启动应用时，新参数必须创建新的终端会话或标签，不能丢失；
* `-pw` 只服务当前连接，不得进入任何持久化数据或 AI 上下文。

## 4. 终端与观察

连接成功后，用户进入可交互 SSH 终端。应用支持会话标签，以及用于同时查看和操作多个主机的九宫格/多主机工作台。

新连接建立后，系统自动执行**只读观察**，获取可用于理解当前环境的基础事实。查询、读取、统计、日志查看和状态检查属于观察能力，可以直接执行；它们不因为辅助驾驶模式而被拦截。

观察的目的不是让模型编写一篇环境摘要作为长期记忆，而是采集稳定、可比较的事实，供事实库直接保存和后续目标驱动任务使用。

## 5. 主机事实库

### 5.1 主机身份与存储形式

主机事实库以**主机名**为唯一记忆关联键，不以 IP 地址作为身份键。IP 可能只是连接路由，特别是在堡垒机或转发场景中并不稳定。

事实库使用结构化数据保存，例如：

* 已安装的软件与版本；
* 进程名称与运行状态；
* 软件安装位置；
* 服务状态；
* 日志位置；
* 必要的配置摘要或配置哈希。

软件、进程名、安装路径等基础事实由系统直接存储和比较，LLM 不负责重新叙述、重建或覆盖这部分长期事实。

### 5.2 更新规则

```text
只读观察
  -> 以主机名查询已有事实
  -> 有记录：与本次结构化事实比较
       -> 有变化：只更新变化字段
       -> 无变化：复用已有事实
  -> 无记录：创建首份结构化事实
```

事实库提供给 AI 的是当前会话相关的事实片段，而不是将密码、私钥、临时连接参数或未经处理的全部终端历史直接交给模型。

## 6. AI 调度与行动

用户以自然语言给出目标。AI 调度与行动负责结合用户目标、当前会话上下文和主机事实，决定需要收集什么证据、如何分解和排序步骤、是否需要协调多台主机，以及如何推进到可验证的结果。

它不被一张固定的“任务类型清单”限制。环境初始化、日志分析、问题诊断和未来扩展能力，都是这种目标驱动调度在不同目标下可以承担的工作，而不是顶层模块或对智能体能力的封顶。

### 6.1 两种驾驶模式

每个会话默认是**辅助驾驶**。用户明确要求升级后，当前会话才进入**全自动驾驶**。

| 模式 | AI 可做的事 | 变更行动 |
| --- | --- | --- |
| 辅助驾驶（默认） | 观察、查询、读取、分析，并以自然语言提出策略与候选命令 | 用户确认候选命令后执行；围栏阻止未经确认的命令逃逸 |
| 全自动驾驶（用户明确升级） | 围绕目标自行收集证据、规划并推进操作 | 可经受控执行入口发起 |

这里的“升级”是用户对当前会话的明确选择，不由 AI 从自然语言自行推断或暗中切换。全自动驾驶适合用户已理解目标、期望 AI 连续推进初始化、修复或部署等工作的场景；辅助驾驶适合用户希望自己掌握所有环境变更的场景。

MVP 不引入自动降级/自动回退、复杂的权限提升弹窗、审计浏览器或策略管理页面。模式切换必须在 UI 中清晰可见，但仅保留完成本流程所需的明确升级动作。

### 6.2 辅助驾驶的确定性正则围栏

辅助驾驶的常规流程是 AI 先用自然语言说明策略并给出候选命令，用户检查后确认执行。用户确认时，Electron 主进程为**当前会话中的这一条精确候选命令**签发一次性的人工确认标志；标志不能由 AI 生成、修改或转用于另一条命令。

安全围栏的目标是防止 AI 绕开这条确认流程而自行下发命令。它是 Electron 主进程内可维护的正则黑名单，不是 AI 提示词、语义分类器或权限策略表单。

```text
辅助驾驶中的 AI 执行请求
  -> 存在与当前会话、精确命令匹配的人工确认标志？
       -> 是：直接执行该命令
       -> 否：以最终命令文本匹配启用的本地正则黑名单
            -> 命中：本地拦截，不发送；AI 改为提示策略
            -> 未命中：正常发送，例如查询命令
```

黑名单只在辅助驾驶中运行；用户明确升级到全自动驾驶后，命令不进入这张黑名单的拦截流程，而是按当前会话的全自动授权经受控执行入口发起。

规则维护在 `设置 > 安全围栏规则` 中完成。每条规则只包含规则名称、正则表达式、启用状态与仅用于验证的匹配测试；所有启用规则的命中结果固定为“拦截，不发送”。规则可以覆盖 `vi` / `vim`、`kill` / `pkill`、重定向、服务启停、脚本包装等命令逃逸路径，但不由 AI 判断是否命中或是否放行。

### 6.3 明显的模式标志

辅助驾驶时，聊天区域、会话标签和多主机卡片持续显示：`辅助驾驶 - 变更需人工确认`。候选命令显示“待确认”状态；用户确认后显示“已人工确认”，并只对被确认的精确命令直接执行。正则黑名单命中时，界面显示拦截规则，同时保留 AI 的提示策略。

全自动驾驶时，同一位置持续显示：`全自动驾驶 - AI 可按当前会话目标执行`。两种模式中，查询命令都可执行；模式标志不应制造“查询也被阻断”的误解。

## 7. MVP 范围

本版本交付的核心是：三种并列连接入口、可交互 SSH 终端和多机会话、自动只读观察、按主机名维护的结构化事实库、OpenAI Chat Completions 模型连接、目标驱动的 AI 调度，以及默认辅助驾驶和用户明确升级至全自动驾驶的控制方式。

以下内容明确不作为当前 MVP 的功能面：

* 持久化凭据保险库或完整凭据管理系统；
* 手工堡垒机跳转配置、供应商堡垒机协议实现，或面向用户的本地 `127.0.0.1` 中转连接入口；
* 自动降级、自动回退和复杂权限策略包；
* 审计系统、审计浏览器和大规模合规管理；
* Ollama 专属 API、运行器集成或模型发现；
* 云、Kubernetes、数据库、CI/CD、容量、安全合规、定时修复等独立产品模块；
* 用固定任务目录限制 AI 可承担的目标类型。

这些排除项不否定未来扩展，而是保证 V2 先把目标驱动智能体、连接兼容、事实沉淀和可理解的驾驶模式做成一个完整而清晰的闭环。

## 8. MVP 验收口径

1. 用户能分别通过用户名密码、私钥和 AccessClient 启动兼容三种并列入口建立会话；私钥格式与口令范围符合本设计。
2. AccessClient 传入的兼容参数能被解析，`tmp:` 临时配置、`-pw`、Raw 无主机名端口场景和后续二次启动均按本设计工作；不存在面向用户的第四种本地中转连接入口。
3. 每次新连接都会执行只读观察；查询命令在辅助驾驶和全自动驾驶中都可以执行。
4. 主机事实只按主机名关联，以结构化字段存储并按比较结果增量更新；不以 IP 为身份键，不让 LLM 重建基础事实。
5. 会话默认显示辅助驾驶。AI 先给出自然语言策略与候选命令；用户确认后，只有带当前会话和精确命令绑定的人工确认标志的命令才能直接执行。
6. 用户明确升级当前会话后，界面持续显示全自动驾驶；AI 才可以经受控入口为该会话推进变更目标。
7. 辅助驾驶中，未带人工确认标志的 AI 自动命令命中任一启用正则规则后，验证为未发送到目标端；未命中规则的查询等命令可正常执行。
8. 大模型连接只使用 OpenAI Chat Completions 协议并支持流式响应，不提供 Ollama 专属接入；AI 调度能围绕用户目标利用当前终端与主机事实推进单机或多机工作，而不依赖固定任务类型清单。
