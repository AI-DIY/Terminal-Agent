### **定位与目标**

将基于 PuTTY 的 AI 助手升级为具备“数字员工/工作搭子”能力的自主 Agent，能上手干活，突破 24 年传统副驾驶水平。


### **一、 核心功能规划**

1. **出外网与文件传输代理**
* **痛点/场景**：内网堡垒机无法出网，但本地 SSH 客户端可出网。
* **实现方式**：AI 在本地接管下载任务（智能替换下载命令），下载完成后通过 SSH 协议二进制打包传输至目标机器。


2. **中央大脑与多机联动控制**
* **界面**：以“友好聊天窗口”为核心交互，配合九宫格 SSH 监控面板。
* **能力**：中央大脑通过聊天窗口统一调度、号召九宫格内所有 SSH 客户端并发/协作执行命令。


3. **初始化环境扫描与记忆沉淀**
* **流程**：连接新环境时，自动扫描所有进程及环境配置，输出总结报告。
* **效果**：将扫描结果沉淀为长期记忆，实现“越用越好用”。


4. **日志分析与事故排查**
* **能力**：基于记忆与分析结果，自动定位日志目录，进行日志分析与告警分析，排查生产问题。


5. **权限分级与安全管控（自主 Agent vs 副驾驶）**
* **高危隔离**：生产部署环境中，严格禁止高危命令、`vi` 编辑及任何有损环境/进程状态的操作。
* **模式降级**：涉及修改生产环境时，AI 自动降级为“副驾驶”，仅提供操作建议，由人工执行。


### **二、 三大全授权/半授权场景划分**

1. **全自动化场景（全授权）**
* **应用**：初次接到环境时的全量进程与配置扫描。
* **机制**：完全授权 AI 自主执行，无需人工干预。


2. **安装任务场景（全授权）**
* **应用**：新机器初始化与软件安装。
* **机制**：提供一键开启开关，开启后全授权自由操作，无需人工审核。


3. **日志分析场景（全授权）**
* **应用**：查看与分析日志、告警排查。
* **机制**：只读无损操作，完全授权 AI 自主执行。


4. **生产变更场景（降级为副驾驶）**
* **应用**：任何涉及修改生产环境的操作。
* **机制**：强制降级为副驾驶，AI 输出建议，禁止自主执行。

### **三、问题与需求整理**

1. **当前现状与痛点**
* **现有实现**：第一版基于 PuTTY 改造成 AI 助手。
* **痛点 1（开发与 UI 限制）**：PuTTY 基于 C 语言编写，很难实现美观的页面与优质的用户体验（UI/UX），目前代码已难以维护和迭代。
* **痛点 2（唤醒与协议交互）**：现有的唤醒机制（如通过 Access Client / PuTTY 命令加载）不够优雅。


2. **技术栈困惑与权衡**
* **技术背景**：自身主要技术栈为 **前端 Vue + 后端 Java**。
* **关于 Electron**：考虑是否换成 Electron 这种前端技术栈来实现，以提升界面和 UX 开发效率，但不限定只用 Electron。
* **关于 Rust**：注意到 CODEX、Claude Desktop 等工具底层使用了 Rust（具备高性能）。但自身没有 Rust 经验，不确定 Rust 是面向过程还是面向对象，担心学习成本、UI 实现及后期升级维护难度，困惑是否有必要强制使用 Rust。


3. **系统定位与核心需求**
* **定位**：打造一个智能体（部分场景下降级为 Copilot 模式）。
* **核心形态**：**纯 UI 界面**（拒绝纯 CLI/GUI 缺乏良好界面的方案，必须融合类似 CODEX 和 Claude Desktop 的体验）。
* **核心能力**：需要将 **SSH 协议**（终端控制）与 AI 功能深度且无缝地融合在一起。


4. **希望获取的技术方案**
* 从 0 到 1 的技术选型与多维度评估（包括架构、开发成本、性能、维护性）。
* 基于开源项目的改造成本评估（是否有成熟开源方案已实现 CODEX 级功能，仅需深度融合 SSH 协议即可）。
