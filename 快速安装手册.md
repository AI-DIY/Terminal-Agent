# Terminal-Agent 3.2.0 快速安装手册

本手册适用于 Windows 独立 ZIP 安装包。ZIP 解压后可以在不依赖仓库目录的情况下完成 AccessClient 中继配置和 Terminal-Agent 安装。

## ZIP 内容

- `putty.exe`：Terminal-Agent 随附的 AccessClient 中继程序。
- `Terminal-Agent-Setup-3.2.0.exe`：Terminal-Agent Windows 安装包。
- `快速安装脚本.cmd`：自动查询 AccessClient、备份原程序、复制中继程序并启动安装包。
- `快速安装手册.md`：本说明的可编辑文本版。
- `快速安装手册.pdf`：适合打印或离线阅读的图文版。

## 安装步骤

1. 将 `Terminal-Agent-Quick-Install-3.2.0.zip` 解压到一个独立文件夹。不要直接在 ZIP 压缩包内运行脚本，也不要把整个文件夹复制到 AccessClient 安装目录。
2. 阅读本手册或打开同目录的 `快速安装手册.pdf`。
3. 双击 `快速安装脚本.cmd`。脚本会按以下顺序执行：
   - 读取注册表 `HKCR\\accessclient\\shell\\open\\command`，获取 AccessClient 的实际安装目录。
   - 将目录中的原 `putty.exe` 备份为 `putty.exe.bak`。已有备份时不会覆盖它。
   - 将 ZIP 内的 `putty.exe` 复制到 AccessClient 目录，并校验复制结果。必要时脚本会请求管理员权限。
   - 最后启动 `Terminal-Agent-Setup-3.2.0.exe`，请在安装向导中自行选择安装目录并完成安装。
4. 安装向导完成后，脚本会启动 Terminal-Agent（可使用 `/NoLaunch` 禁止自动启动）。

如果 AccessClient 没有注册 `accessclient` 协议，或者注册表路径已失效，可以在命令行中使用 `/AccessClientDir "D:\\AccessClient"` 指定目录。离线环境可使用 `/NoDownload`，此时脚本只使用 ZIP 内的文件。

## 【配置须知】

在 AccessClient 的“会话配置”中，“会话访问方式”必须选择“使用全局设置(putty)”。这是 AccessClient 的厂商界面设置，快速安装脚本不会修改未知的厂商配置文件。

## 【使用须知】

使用集团堡垒机正常指定主机 SSH 连接即可。之后 AccessClient 会通过已替换的 `putty.exe` 中继程序唤起 Terminal-Agent。

## 备份和恢复

脚本会保留原文件 `putty.exe.bak`。如需还原，请先退出 AccessClient，再删除中继文件并将备份文件改回 `putty.exe`。如果脚本在安装包启动前失败，它会尝试自动恢复本次运行创建的备份。

## 常用参数

```text
快速安装脚本.cmd /?
快速安装脚本.cmd /NoDownload
快速安装脚本.cmd /NoLaunch
快速安装脚本.cmd /NoPause
快速安装脚本.cmd /AccessClientDir "D:\\AccessClient"
```

脚本会在复制中继程序成功后才启动安装包，避免安装失败时误报已完成。请不要将不明来源的 `putty.exe` 或安装包替换到 ZIP 中。
