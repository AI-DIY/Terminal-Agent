# Terminal-Agent 调试指南手册

本文面向需要排查 Terminal-Agent 前端、主进程、SSH 通道、AI 请求和本机 Skill 的开发与测试人员。指南适用于开发模式和 Windows 安装包；涉及真实主机、员工工号、令牌、终端输出或模型密钥时，请先脱敏，并只在授权环境中调试。

## 1. 先确认运行形态与复现条件

开发模式使用仓库根目录启动：

```powershell
npm install
npm run dev
```

安装包问题要在 `release\win-unpacked` 或已安装的 `Terminal-Agent-runtime.exe` 中复现。开发服务器和安装包的资源路径不同，尤其是外置 `.skills` 目录：

- 开发模式：项目根目录 `.skills\<skill-name>\SKILL.md`；
- 安装模式：`Terminal-Agent-runtime.exe` 同级 `.skills\<skill-name>\SKILL.md`；
- 用户启停状态：Electron `userData\skills-state.json`，不会随技能目录删除而自动清空。

每次复现记录应用版本、主题、窗口尺寸、是否登录、在线 SSH 数量、使用的模型提供方，以及能稳定触发问题的最短操作序列。悬浮和输入问题优先用相同窗口尺寸连续复现 10 秒，避免凭感觉比较延迟。

## 2. 打开 DevTools：排查前端与 IPC

在工作台右上角点击 **DevTools**，或按 `Ctrl+Shift+I`。入口打开的是当前渲染器的独立 Chromium DevTools；再次点击会复用同一窗口，关闭后可以重新打开。

### 2.1 Console：确认页面状态与异常

先查看红色异常和未处理 Promise，再检查 Vue 组件是否重复挂载。可以使用以下只读命令：

```js
document.activeElement?.outerHTML
performance.getEntriesByType('measure').slice(-20)
```

不要在 Console 粘贴来源不明的脚本。调试完成后清理包含用户数据的 Console、Network 和截图。

### 2.2 Network：前端调用后端的入口

Terminal-Agent 的渲染器不是通过浏览器 REST API 直接访问主进程，而是通过 preload 暴露的 `window.terminalAgent` API，再由 Electron IPC 到主进程。Network 面板通常看不到这些 IPC 调用，因此应配合 IPC 时序日志：

```js
window.terminalAgent.diagnostics.setIpcTracing(true)
```

随后执行一次最短复现，并在 Console 过滤 `[TA IPC]`。每条记录只包含关联编号、IPC 通道、成功/失败和往返耗时，不记录参数、响应正文、路径、密钥或完整错误原文。例如：

```text
[TA IPC #12] sessions:write completed 2ms
[TA IPC #13] chat:send failed 1840ms
```

关闭时序日志：

```js
window.terminalAgent.diagnostics.setIpcTracing(false)
```

排查方向：

1. `sessions:write` 很快，但 SSH 远端回显慢：继续查 SSH 连接、PTY 或远端 Shell。
2. `sessions:write` 本身耗时：查主进程事件循环、同步文件操作或过多 IPC 调用。
3. `chat:send` 很快失败：查参数校验、SSO 技能权限或模型配置。
4. `chat:send` 长时间 pending：查模型网络、超时和 `ChatRuntime` 的取消/重试路径。
5. `skills:load` 成功而 `skills:run` 失败：先看执行程序、工作目录和退出码，再看脚本自身错误。

## 3. Node Inspector：排查主进程后端逻辑

点击 **Node Inspector** 会为当前主进程创建仅监听 `127.0.0.1` 的 Inspector 窗口。它适合调试以下代码：

- IPC handler：`src/main/ipc/`、`src/main/chat/register-chat-handlers.ts`、`src/main/skills/register-skill-handlers.ts`；
- SSH 生命周期：`src/main/ssh/session-service.ts` 和 `ssh2-client-adapter.ts`；
- AI 运行时：`src/main/chat/chat-runtime.ts`、`structured-chat-agent.ts`；
- Skill 发现/执行：`src/main/skills/skill-service.ts`、`skill-command-executor.ts`；
- 设置、状态持久化和安装包路径解析。

推荐的断点顺序是“入口 → 校验 → 核心服务 → 返回值”：

1. 在对应 `ipcMain.handle` 的第一行断点，确认请求确实进入主进程且 sender 可信。
2. 在 Zod `parse` 之后查看已校验输入；不要修改校验器绕过安全边界。
3. 在服务方法的外部 IO 前后断点，比较等待时间和异常。
4. 在 handler 返回前确认返回对象满足共享 contract；渲染器会再次校验。

Node Inspector Console 中可以检查：

```js
process.pid
process.execPath
process.env.NODE_ENV
```

不要在 Inspector 中打印或复制 `process.env`、模型 API key、SSH 密码、私钥、终端完整输出或用户目录文件。关闭 Inspector 后，应用会回收本次启动创建的窗口。

## 4. 调试 SSH 工作区输入

SSH 终端输入路径是：

```text
xterm.onData
  → window.terminalAgent.sessions.write
  → ipcMain sessions:write
  → SessionService.write
  → SshShell.write
```

回显路径是：

```text
SSH channel data
  → SessionService.publishData
  → sessions:data IPC
  → WorkbenchView 的按帧批处理
  → TerminalPane / xterm.write
```

排查输入卡顿时：

1. 打开 DevTools Performance，录制 5 秒连续输入，不要同时录制整个屏幕。
2. 查看 Long Task 是否集中在渲染器布局、xterm 绘制、Vue 响应式更新或 IPC 回调。
3. 使用 IPC tracing 比较 `sessions:write` 往返时间；按键输入不能依赖人为 `setTimeout` 延迟。
4. 检查终端是否发生 `fit.fit()`、ResizeObserver 循环或工作区布局重排。
5. 关闭广播、文件传输面板和其他非必要面板后复现，以区分单个终端问题和整体布局压力。

输入事件应保持即时发送；终端输出可以合并到动画帧，但不能丢字符、重复回显或改变 Ctrl+C、退格、方向键等控制字符。修复后用普通文字、中文、组合键、粘贴和快速连续 100 个字符分别验证。

## 5. 调试 AI 工作区输入和聊天

AI 输入框的链路是：

```text
textarea input
  → global-chat store draft
  → Enter / 发送
  → chat:send IPC
  → ChatRuntime
  → StructuredChatAgent / provider stream
```

键入期间不应触发模型请求、磁盘写入或完整聊天上下文重算。Performance 录制中重点查看：

- `input` 事件是否每个字符触发昂贵的深层 watcher；
- 消息流式更新是否导致整个 transcript 重新布局；
- 自动滚动是否在同一帧重复读写 `scrollHeight`；
- `$` 技能候选过滤是否只更新小的本地列表；
- 发送后是否只清理草稿，而不是同步重建整个工作区。

如果 `chat:send` 已经发出，继续查主进程：先看 `chat:progress`，再看模型请求耗时、上下文 token 估算、Skill 动作和最终 `chat:completed`/`chat:error`。取消时应看到 `chat:cancel` 返回，且不会残留思考卡或 Skill 执行卡。

## 6. 调试 Dynamic Skill（以 query-system-inspection 为例）

### 6.1 发现与启停

确认目录结构完整：

```text
.skills\query-system-inspection\
  SKILL.md
  scripts\query-system.js
  references\api.md
  agents\openai.yaml
```

打开“技能”页点击“刷新”，确认技能名称、描述和启用状态出现。格式错误目录应出现在诊断区域，不应阻塞其他技能。禁用后重启应用，状态应由 `userData\skills-state.json` 恢复。

### 6.2 显式选择与执行闭环

在 AI 工作区输入 `$`，选择 `query-system-inspection`，再发送包含工号的请求。完整闭环应为：

```text
选择 → skills:load → 模型返回 run_skill_command
→ skills:run（固定在该技能目录）→ stdout/stderr/退出码回灌
→ 模型整理最终 reply
```

本技能的本机脚本不需要在线 SSH。先查询应用列表：

```text
node scripts/query-system.js --list e0128483
```

查询报告前必须使用同一工号和列表返回的 `bizId` 再执行：

```text
node scripts/query-system.js --report e0128483 89 1 2026-09-06
```

如果卡片显示“执行失败”，检查卡片中的阶段、退出码和 stderr；不要只看模型最终一句话。常见原因是脚本缺失、路径不在技能目录、Node/curl 不在 PATH、网络/TLS/HTTP 错误、业务 `code` 非零或日期参数不合法。

### 6.3 失败隔离与安全边界

`SKILL.md` 是说明，不是自动执行脚本。只有模型明确返回的单个 `run_skill_command` 才会进入本机执行器；读取、刷新、启停和加载正文不会执行目录中的代码。执行器提供超时、取消和输出上限，报告 HTML 只能作为数据分析，不能成为新的命令。

## 7. 修改后的自测清单

每次修复至少运行：

```powershell
npm run lint
npm test
npm run build
```

人工验证：

- 鼠标从工作区其他区域连续移入“技能”“设置”“DevTools”“Node Inspector”，悬浮样式应在同一帧出现，移出后立即消失；
- SSH 终端快速键入、中文输入、Ctrl+C、退格、方向键和粘贴均无明显延迟；
- AI 工作区快速键入、`$` 候选、Shift+Enter 换行和 Enter 发送均无卡顿；
- Skill 显式选择能看到“加载中/执行中/整理结果”卡片，成功或失败后卡片都会清理；
- 关闭登录门控时技能入口和技能动作仍被拒绝；
- 安装包同级 `.skills` 复制/刷新后能被发现，且普通聊天不受坏目录影响。

日志、截图和安装包交付前，删除调试输出中的身份、地址、凭据、报告正文和私有终端内容。
